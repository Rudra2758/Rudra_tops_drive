#include<stdio.h>
#include<math.h>

main(){
	int a,b;
	
	printf("the number  of a is :");
	scanf("%d",&a);
	
	pirntf("the power is :");
	scanf("%d",&b);
	
    int sum;
    sum=pow(a,b);

    printf("the sum is :");
    return 0;
}
